/** @type {import('tailwindcss').Config} */
config.colors = require('tailwindcss/colors')

module.exports = {
  content: ["./public/**/*.{html,js,css}"],
  theme: {
    extend: {
      colors: {
        },
      },
    },
  plugins: [],
  }

